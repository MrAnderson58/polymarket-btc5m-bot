# Copy to .env and adjust before: docker compose up -d
POSTGRES_USER=trading
POSTGRES_PASSWORD=changeme
POSTGRES_DB=trading_ai
POSTGRES_PORT=5432
ADMINER_PORT=8080

# Application (.env in project root):
# MARKET_EVENTS_DB_BACKEND=postgresql
# MARKET_EVENTS_DB_URL=postgresql://trading:changeme@localhost:5432/trading_ai
